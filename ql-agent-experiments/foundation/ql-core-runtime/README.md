# Direct Core QL runtime

This directory owns the #97 Direct Core recurrence semantics behind the shared LoopRuntime seam.

The implementation target preserves a real Circuit; the P0/P5 implicate versus P1–P4 explicate distinction; all validated Rij relations; π 0/1 projection and ρ 1/0 return; interpretation of returned difference before transition; six typed residue responsibilities; explicit P5 determination; distinct R51/R52/R53/R54/R50 reopening; positive QLClosure; and retained ReentryDelta conditioning renewed P0+.

It must not become a six-step scheduler, derive QL position from a carrier, equate P5 with stop, or equate tool absence with closure. Conjugation and recursive depth remain later extension seams.
