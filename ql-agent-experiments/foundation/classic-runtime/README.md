# Classic runtime

This directory owns the deliberately small non-QL control condition for #96. It uses the shared RuntimeHost boundary and ordinary model/capability/follow-up recurrence. It must not acquire QL position, residue, determination or closure semantics.

Executable source for this directory is currently blocked from publication by the connected GitHub write guard in this session. See `../FOUNDATION-BLOCKER.md`.
