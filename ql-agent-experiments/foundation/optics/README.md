# Foundation optics

This directory owns the #98 shared run manifest and observation surface: stable run identity, held-variable manifests, separate raw host events and runtime-semantic events, replay without a live session, and matched `classic` versus `ql-core` comparison records.

Executable source is currently blocked from publication by the connected GitHub write guard in this session. See `../FOUNDATION-BLOCKER.md`.
