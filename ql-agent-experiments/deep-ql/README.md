# Deep QL workbench — reserved

This directory is part of the #95 programme workspace only. **No Deep QL implementation begins on `ql/runtime-foundation`.**

After a valid Foundation Freeze, the development protocol allows the separate `ql/deep-runtime` track to materialise the required areas for conjugation, recursive depth, conformance, typing corpus, rendering, extensions and research. Until #100 is closed, this directory is only the branch-point marker and dependency boundary.
